global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'otel-collector' # Cambia el nombre del trabajo para que sea más claro
    static_configs:
      - targets: ['otel-collector:9090'] # Asegúrate de que el objetivo sea el colector